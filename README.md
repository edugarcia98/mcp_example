How to install:

```json
{
  "mcpServers": {
    "server": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/edugarcia98/mcp_example.git",
        "mcp-server"
      ]
    }
  }
}
```